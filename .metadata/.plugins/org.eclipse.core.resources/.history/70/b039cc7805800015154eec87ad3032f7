import unittest
from classes import *

class test_province_count(unittest.TestCase):
    def setUp(self):
        global provnum
        self.assertEqual(Province.total_provinces,provnum)
                     